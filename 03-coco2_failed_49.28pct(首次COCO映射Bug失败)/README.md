# COCO 首次训练 — 49.28%（失败）

| 项目 | 内容 |
|------|------|
| 模型 | YOLO11s |
| 数据 | COCO 2017（118,287张） |
| mAP50 | **49.28%** |
| 失败原因 | 类别映射Bug |

🐛 **Bug：** `prepare_coco.py` 写死 `C={i+1:i}`，但COCO原版ID有断号
（1,2,3,...,11,13,14,...,90），导致类别错乱。
修复后见 `coco3_fixed_61.81pct/`。
